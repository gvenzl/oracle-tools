# Oracle Database Linux Systemd service

This will install a `systemd` service called `oracle-database` for Oracle Database.

Run the `install.sh` shell script. You will need `sudo` privileges or run as `root`.
